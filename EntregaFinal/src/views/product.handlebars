<div class="container">
  <nav>
  <ul>
    <li><a href="/">HOME</a></li>
    <li><a href="/products">PRODUCTOS</a></li>
    <li><a href="/cart/646267ba8cc04a3efca60438">CARRITO</a></li>
  </ul>
  <div class="user-dropdown">
    <button class="user-dropdown-btn"> {{user.role}}</button>
    <div class="user-dropdown-content">
      <p class="users">User: {{user.name}}</p>
      <p>Email: {{user.email}}</p>
      <a href="/profile"><button>Mi perfil</button> </a>
      <a href="/api/session/logout">
        <button>Cerrar Sesión</button>
      </a>
    </div>
  </div>
</nav>
    <h1 class="title">VISTA UNICA PRODUCTO</h1>
    <div class="formulario"></div>
    {{#if message}}
  <div class="alert alert-warning">{{message}}</div>
    {{/if}}
  <div class="gallery">
      <div class="product">
        <h2>{{this.product.title}}</h2>
          <img src="{{this.product.img}}" alt="{{this.product.title}}">
        <p>{{this.product.description}}</p>
        <p>Category: {{this.product.category}}</p>
        <p>Price: {{this.product.price}}</p>
        <p>Stock: {{this.product.stock}}</p>
        <p>Disponibilidad: {{this.product.status}}</p>
        <button>Agregar al carrito</button>
      </div>
  </div>